import styled from 'styled-components';
import { Button, Layer as LayerBase } from 'grommet';

export const CloseButton = styled(Button)`
  position: absolute;
  top: 0;
  right: 0;
`;

export const Layer = styled(LayerBase)`
  
`;