import styled from 'styled-components';

export const Container = styled.div`
    padding: 0;
`;

export const StandardContentPage = styled.div`
  padding-bottom: 0;
`;

export const ContentContainer = styled.div`

`;
